<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.labor.management.mapper.StudentMapper">

    <!-- 公共字段映射片段（总表/助教查询）：公司 → 班级(W-S-E) → 学生(班内编号) -->
    <sql id="masterListColumns">
        s.id                                                          AS id,
        s.student_id                                                  AS studentId,
        s.name                                                        AS name,
        s.gender                                                      AS gender,
        s.class_id                                                    AS classId,
        c.company_id                                                  AS companyId,
        comp.name                                                     AS companyName,
        c.week                                                        AS week,
        c.start_session                                               AS startSession,
        c.end_session                                                 AS endSession,
        c.class_code                                                  AS classCode,
        c.class_name                                                  AS className,
        s.student_no_in_class                                         AS studentNoInClass,
        CONCAT_WS('-', c.class_code, s.student_no_in_class)           AS fullNo,
        s.original_major                                              AS originalMajor,
        s.is_assistant                                                AS isAssistant,
        CASE WHEN s.is_assistant = 1 THEN '助教' ELSE '学生' END       AS identity,
        (SELECT GROUP_CONCAT(cc.class_name ORDER BY cc.company_id, cc.week, cc.start_session SEPARATOR '、')
           FROM assistant_class ac
           JOIN classes cc ON cc.id = ac.class_id AND cc.deleted = 0
           WHERE ac.assistant_student_id = s.id AND ac.deleted = 0)   AS assignedClassNames,
        CASE WHEN u.id IS NOT NULL THEN 1 ELSE 0 END                  AS hasAccount
    </sql>

    <!-- 总表关联：classes(所属班级) + company(公司) + sys_user(账号) -->
    <sql id="masterListJoins">
        LEFT JOIN classes c ON s.class_id = c.id AND c.deleted = 0
        LEFT JOIN company comp ON comp.id = c.company_id AND comp.deleted = 0
        LEFT JOIN sys_user u ON u.student_id = s.id AND u.deleted = 0
    </sql>

    <!-- 分页查询学生（关联班级表获取班级名称） -->
    <select id="selectStudentPage" resultType="com.labor.management.vo.StudentVO">
        SELECT
            s.id,
            s.student_id      AS studentId,
            s.name,
            s.class_id        AS classId,
            c.class_name      AS className,
            s.gender,
            s.status,
            s.created_at      AS createdAt
        FROM student s
        LEFT JOIN classes c ON s.class_id = c.id
        WHERE s.deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (s.student_id LIKE CONCAT('%', #{keyword}, '%')
                 OR s.name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="classId != null">
            AND s.class_id = #{classId}
        </if>
        <if test="scopeClassIds != null">
            <choose>
                <when test="scopeClassIds.size() == 0">
                    AND 1 = 0
                </when>
                <otherwise>
                    AND s.class_id IN
                    <foreach collection="scopeClassIds" item="cid" open="(" separator="," close=")">
                        #{cid}
                    </foreach>
                </otherwise>
            </choose>
        </if>
        ORDER BY s.created_at DESC
    </select>

    <!-- 总表分页查询（公司 → 班级 W-S-E → 班内编号 排序） -->
    <select id="selectMasterListPage" resultType="com.labor.management.vo.MasterListViewVO">
        SELECT
        <include refid="masterListColumns"/>
        FROM student s
        <include refid="masterListJoins"/>
        WHERE s.deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (s.student_id LIKE CONCAT('%', #{keyword}, '%')
                 OR s.name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="companyId != null">
            AND c.company_id = #{companyId}
        </if>
        <if test="classId != null">
            AND s.class_id = #{classId}
        </if>
        <if test="identity != null and identity == 'ASSISTANT'">
            AND s.is_assistant = 1
        </if>
        <if test="identity != null and identity == 'STUDENT'">
            AND s.is_assistant = 0
        </if>
        <if test="scopeClassIds != null">
            <choose>
                <when test="scopeClassIds.size() == 0">
                    AND 1 = 0
                </when>
                <otherwise>
                    AND s.class_id IN
                    <foreach collection="scopeClassIds" item="cid" open="(" separator="," close=")">
                        #{cid}
                    </foreach>
                </otherwise>
            </choose>
        </if>
        ORDER BY comp.sort_order, c.week, c.start_session, c.end_session, s.student_no_in_class, s.id
    </select>

    <!-- 助教分页查询（is_assistant=1，含负责班级数量与聚合名称） -->
    <select id="selectAssistantPage" resultType="com.labor.management.vo.AssistantVO">
        SELECT
        <include refid="masterListColumns"/>,
        (SELECT COUNT(*) FROM assistant_class ac
           WHERE ac.assistant_student_id = s.id AND ac.deleted = 0)  AS assignedClassCount
        FROM student s
        <include refid="masterListJoins"/>
        WHERE s.deleted = 0
          AND s.is_assistant = 1
        <if test="keyword != null and keyword != ''">
            AND (s.student_id LIKE CONCAT('%', #{keyword}, '%')
                 OR s.name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY comp.sort_order, c.week, c.start_session, s.student_no_in_class, s.id
    </select>

    <!-- 查询尚未分配任何负责班级的助教列表（多对多：assistant_class 无记录） -->
    <select id="selectUnassignedAssistants" resultType="com.labor.management.vo.MasterListViewVO">
        SELECT
        <include refid="masterListColumns"/>
        FROM student s
        <include refid="masterListJoins"/>
        WHERE s.deleted = 0
          AND s.is_assistant = 1
          AND NOT EXISTS (
              SELECT 1 FROM assistant_class ac
              WHERE ac.assistant_student_id = s.id AND ac.deleted = 0
          )
        ORDER BY s.created_at DESC
    </select>

</mapper>
